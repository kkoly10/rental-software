node_modules
.next
out
dist
coverage
*.tsbuildinfo
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.vercel
*.log
*.zip
.DS_Store
next-env.d.ts
.claude/
playwright-report/
playwright-e2e-report/
test-results/
.env.e2e.local

# Supabase CLI scratch — created by `supabase link` and `db push` in CI.
# config.toml is committed (project_id is non-secret); .temp holds the
# linked access token + db password.
supabase/.temp/
supabase/.branches/
